# GENERATED by scripts/gen_compose.py from context/scopes.json — DO NOT EDIT.
# One claude runner per isolation root; each mounts only its ancestor own-
# content (ro) + its own subtree (rw). Use with the base file at cutover:
#   docker compose -f docker-compose.yml -f compose.scopes.yml up
services:
  claude-runner-enterprise:
    build: ./claude-runner
    container_name: claude-runner-enterprise
    restart: unless-stopped
    cap_add:
    - NET_ADMIN
    environment:
    - RUNNER_TOKEN=${RUNNER_TOKEN:?set RUNNER_TOKEN in .env}
    - FIREWALL=on
    - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
    - DEFAULT_MAX_TURNS=${DEFAULT_MAX_TURNS:-40}
    - FANOUT_CONCURRENCY=${FANOUT_CONCURRENCY:-4}
    - N8N_API_URL=${N8N_API_URL:-}
    - N8N_API_KEY=${N8N_API_KEY:-}
    - ASK_HUMAN_URL=${ASK_HUMAN_URL:-http://n8n:5678/webhook/ask-human}
    - ASK_HUMAN_TOKEN=${ASK_HUMAN_TOKEN:?set a DISTINCT ASK_HUMAN_TOKEN in .env}
    - SCOPE_ROOT=scopes/enterprise
    - PII_POLICY=block
    volumes:
    - claude-home:/home/node
    - claude-config:/home/node/.claude
    - ./workspace/scopes/enterprise/CLAUDE.md:/workspace/scopes/enterprise/CLAUDE.md
    networks:
    - agentnet
    mem_limit: 3g
    memswap_limit: 3g
    cpus: '2.0'
    pids_limit: 512
  claude-runner-consulting:
    build: ./claude-runner
    container_name: claude-runner-consulting
    restart: unless-stopped
    cap_add:
    - NET_ADMIN
    environment:
    - RUNNER_TOKEN=${RUNNER_TOKEN:?set RUNNER_TOKEN in .env}
    - FIREWALL=on
    - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
    - DEFAULT_MAX_TURNS=${DEFAULT_MAX_TURNS:-40}
    - FANOUT_CONCURRENCY=${FANOUT_CONCURRENCY:-4}
    - N8N_API_URL=${N8N_API_URL:-}
    - N8N_API_KEY=${N8N_API_KEY:-}
    - ASK_HUMAN_URL=${ASK_HUMAN_URL:-http://n8n:5678/webhook/ask-human}
    - ASK_HUMAN_TOKEN=${ASK_HUMAN_TOKEN:?set a DISTINCT ASK_HUMAN_TOKEN in .env}
    - SCOPE_ROOT=scopes/enterprise/consulting
    - PII_POLICY=block
    volumes:
    - claude-home:/home/node
    - claude-config:/home/node/.claude
    - ./workspace/scopes/enterprise/CLAUDE.md:/workspace/scopes/enterprise/CLAUDE.md:ro
    - ./workspace/scopes/enterprise/consulting/CLAUDE.md:/workspace/scopes/enterprise/consulting/CLAUDE.md
    networks:
    - agentnet
    mem_limit: 3g
    memswap_limit: 3g
    cpus: '2.0'
    pids_limit: 512
  claude-runner-tenant-a:
    build: ./claude-runner
    container_name: claude-runner-tenant-a
    restart: unless-stopped
    cap_add:
    - NET_ADMIN
    environment:
    - RUNNER_TOKEN=${RUNNER_TOKEN:?set RUNNER_TOKEN in .env}
    - FIREWALL=on
    - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
    - DEFAULT_MAX_TURNS=${DEFAULT_MAX_TURNS:-40}
    - FANOUT_CONCURRENCY=${FANOUT_CONCURRENCY:-4}
    - N8N_API_URL=${N8N_API_URL:-}
    - N8N_API_KEY=${N8N_API_KEY:-}
    - ASK_HUMAN_URL=${ASK_HUMAN_URL:-http://n8n:5678/webhook/ask-human}
    - ASK_HUMAN_TOKEN=${ASK_HUMAN_TOKEN:?set a DISTINCT ASK_HUMAN_TOKEN in .env}
    - SCOPE_ROOT=scopes/enterprise/consulting/tenant-a
    - PII_POLICY=block
    volumes:
    - claude-home:/home/node
    - claude-config:/home/node/.claude
    - ./workspace/scopes/enterprise/CLAUDE.md:/workspace/scopes/enterprise/CLAUDE.md:ro
    - ./workspace/scopes/enterprise/consulting/CLAUDE.md:/workspace/scopes/enterprise/consulting/CLAUDE.md:ro
    - ./workspace/scopes/enterprise/consulting/tenant-a:/workspace/scopes/enterprise/consulting/tenant-a
    networks:
    - agentnet
    mem_limit: 3g
    memswap_limit: 3g
    cpus: '2.0'
    pids_limit: 512
  claude-runner-tenant-b:
    build: ./claude-runner
    container_name: claude-runner-tenant-b
    restart: unless-stopped
    cap_add:
    - NET_ADMIN
    environment:
    - RUNNER_TOKEN=${RUNNER_TOKEN:?set RUNNER_TOKEN in .env}
    - FIREWALL=on
    - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
    - DEFAULT_MAX_TURNS=${DEFAULT_MAX_TURNS:-40}
    - FANOUT_CONCURRENCY=${FANOUT_CONCURRENCY:-4}
    - N8N_API_URL=${N8N_API_URL:-}
    - N8N_API_KEY=${N8N_API_KEY:-}
    - ASK_HUMAN_URL=${ASK_HUMAN_URL:-http://n8n:5678/webhook/ask-human}
    - ASK_HUMAN_TOKEN=${ASK_HUMAN_TOKEN:?set a DISTINCT ASK_HUMAN_TOKEN in .env}
    - SCOPE_ROOT=scopes/enterprise/consulting/tenant-b
    - PII_POLICY=block
    volumes:
    - claude-home:/home/node
    - claude-config:/home/node/.claude
    - ./workspace/scopes/enterprise/CLAUDE.md:/workspace/scopes/enterprise/CLAUDE.md:ro
    - ./workspace/scopes/enterprise/consulting/CLAUDE.md:/workspace/scopes/enterprise/consulting/CLAUDE.md:ro
    - ./workspace/scopes/enterprise/consulting/tenant-b:/workspace/scopes/enterprise/consulting/tenant-b
    networks:
    - agentnet
    mem_limit: 3g
    memswap_limit: 3g
    cpus: '2.0'
    pids_limit: 512
  claude-runner-engineering:
    build: ./claude-runner
    container_name: claude-runner-engineering
    restart: unless-stopped
    cap_add:
    - NET_ADMIN
    environment:
    - RUNNER_TOKEN=${RUNNER_TOKEN:?set RUNNER_TOKEN in .env}
    - FIREWALL=on
    - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
    - DEFAULT_MAX_TURNS=${DEFAULT_MAX_TURNS:-40}
    - FANOUT_CONCURRENCY=${FANOUT_CONCURRENCY:-4}
    - N8N_API_URL=${N8N_API_URL:-}
    - N8N_API_KEY=${N8N_API_KEY:-}
    - ASK_HUMAN_URL=${ASK_HUMAN_URL:-http://n8n:5678/webhook/ask-human}
    - ASK_HUMAN_TOKEN=${ASK_HUMAN_TOKEN:?set a DISTINCT ASK_HUMAN_TOKEN in .env}
    - SCOPE_ROOT=scopes/enterprise/engineering
    - PII_POLICY=block
    volumes:
    - claude-home:/home/node
    - claude-config:/home/node/.claude
    - ./workspace/scopes/enterprise/CLAUDE.md:/workspace/scopes/enterprise/CLAUDE.md:ro
    - ./workspace/scopes/enterprise/engineering:/workspace/scopes/enterprise/engineering
    networks:
    - agentnet
    mem_limit: 3g
    memswap_limit: 3g
    cpus: '2.0'
    pids_limit: 512
